# This rule is not used by the project velkir itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the velkir.ioxie.dev.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: velkir
    app.kubernetes.io/managed-by: kustomize
  name: valkey-editor-role
rules:
- apiGroups:
  - velkir.ioxie.dev
  resources:
  - valkeys
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - velkir.ioxie.dev
  resources:
  - valkeys/status
  verbs:
  - get
